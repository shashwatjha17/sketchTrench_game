# Copy to .env and fill in if needed. The backend reads these via ${VAR:default} syntax in application.yml.
# All values have working defaults for local dev, so this file is optional.

# ---- Server ----------------------------------------------------------
PORT=8080
SPRING_PROFILES_ACTIVE=dev
CORS_ALLOWED_ORIGINS=http://localhost:5173
